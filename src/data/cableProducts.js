/** @type {import('./products').Product[]} — populated via Admin → Category pages. */
export const cableProducts = []

/** Full-bleed hero carousel (same shape as home `HeroSection` slides). */
export const cablesHeroSlides = []

/** Filled from Admin → Category pages (localStorage). Empty until saved. */
export const cablesPageMeta = {
  title: '',
  blurb: '',
}
