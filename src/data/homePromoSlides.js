/**
 * Home hero promo carousel — premium card slides (headphones / earbuds).
 * Defaults live in {@link ./homePromoCarouselDefaults.js}; admin overrides via siteStore.
 */

export { defaultHomePromoSlides as homePromoSlides } from './homePromoCarouselDefaults'

/** @typedef {import('./homePromoCarouselDefaults').HomePromoSlide} HomePromoSlide */
