/** @type {import('./products').Product[]} — populated via Admin → Category pages. */
export const batteryProducts = []

/** Full-bleed hero carousel (same shape as home `HeroSection` slides). */
export const batteriesHeroSlides = []

export const batteriesPageMeta = {
  title: 'Batteries & Power',
  blurb:
    'Power banks, replacement cells, rechargeables and MagSafe-compatible packs — reliable capacity, safe charging cycles and portable designs for phones, laptops and everyday gadgets.',
}
